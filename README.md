# Toolathlon ML promotion-review evidence

Synthetic, non-production evidence for a bounded benchmark task.
Canonical recipe records are under `recipes/`; the approved registry is `registry/approved_recipes.json`; scoped exceptions are GitHub issues whose titles begin with `EXC-`.
